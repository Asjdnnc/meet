version: "3.8"

services:
  backend:
    build: ./backend
    container_name: meet-backend
    ports:
      - "8080:8080"                       # Expose backend port
    env_file:
      - ./backend/.env                   # Load env vars from file
    restart: always
